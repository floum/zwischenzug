<div class="scoresheet">
  <table>
  <% moves.each_slice(2).each_with_index do |(white_ply, black_ply), index| %>
    <tr>
      <td><%= index + 1 %>.</td>
      <td><%= @translator.to(white_ply) %><td>
      <td><%= @translator.to(black_ply) %></td>
    </tr>
  <% end %>
  </table>
</div>
